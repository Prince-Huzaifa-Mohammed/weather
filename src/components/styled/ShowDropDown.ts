import styled from "styled-components";

export const ShowDropDown = styled.div`
  opacity: 1;
  pointer-events: auto;
  visibility: visible;
`;

export const HideDropDown = styled.div`
  opacity: 0;
  pointer-events: none;
  visibility: hidden;
`;
