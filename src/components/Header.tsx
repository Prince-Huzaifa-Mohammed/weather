import React from "react";
import { StyledHeader } from "./styled/Header";

const Header = () => {
  return (
    <StyledHeader>
      <h1>Header</h1>
    </StyledHeader>
  );
};

export default Header;
